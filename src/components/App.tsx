{"import React from 'react';
import ReconnectionHandler from './ReconnectionHandler';

const App: React.FC = () => {
  return (
    <ReconnectionHandler>
      {/* Your app content here */}
    </ReconnectionHandler>
  );
};
export default App;