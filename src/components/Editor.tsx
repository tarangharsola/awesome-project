{"import React from 'react';
import { useCursor } from './useCursor';

interface EditorProps {
  cursor: useCursor;
}

const Editor = ({ cursor }: EditorProps) => {
  // implementation...
}

export default Editor;