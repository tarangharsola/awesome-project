import React, { useState, useEffect } from 'react';import CodeMirror from 'codemirror';const Editor = ({ roomId }) => {  const [code, setCode] = useState('');  useEffect(() => {    const socket = io();    socket.on('code', (code) => {      setCode(code);    });  }, []);  return (    <div>      <CodeMirror value={code} onChange={(code) => {        socket.emit('code', code);      }} />    </div>  );};export default Editor;