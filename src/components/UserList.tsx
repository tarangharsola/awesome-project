{"import React from 'react';\nimport { User } from '\"/src/components/User.tsx\"';\n\ninterface Props {\n  users: User[];\n}\n\nconst UserList: React.FC<Props> = ({ users }) => {\n  return (\n    <div>\n      {users.map((user) => (\n        <div key={user.id}>\n          <span>\n            {user.name}\n          </span>\n          <span>\n            {user.color}\n          </span>\n        </div>\n      ))}\n    </div>\n  );\n};\n\nexport default UserList;